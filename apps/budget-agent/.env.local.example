# Anthropic API key for the budget-agent copilot (server-side only, never expose to the client).
ANTHROPIC_API_KEY=

# HeroUI Pro CLI auth token — required only to pull licensed Pro components/templates via `npx heroui-pro@latest`.
HEROUI_AUTH_TOKEN=
